<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:fwc="clr-namespace:Digimezzo.Foundation.WPF.Controls;assembly=Digimezzo.Foundation.WPF">

    <Style x:Key="MaterialTextBoxLightStyle" TargetType="{x:Type fwc:MaterialTextBox}">
        <Setter Property="Accent" Value="{DynamicResource Brush_PrimaryAccent}"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush_DarkText}"/>
        <Setter Property="IsFloating" Value="True"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeNormal}"/>
    </Style>

    <Style x:Key="MaterialTextBoxLightStyleLarge" TargetType="{x:Type fwc:MaterialTextBox}" BasedOn="{StaticResource MaterialTextBoxLightStyle}">
        <Setter Property="FontSize" Value="{DynamicResource FontSizeLarge}"/>
    </Style>

    <Style x:Key="MaterialTextBoxDarkStyle" TargetType="{x:Type fwc:MaterialTextBox}">
        <Setter Property="Accent" Value="{DynamicResource Brush_PrimaryAccent}"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush_LightText}"/>
        <Setter Property="ErrorForeground" Value="{DynamicResource Brush_SecondaryAccent}"/>
        <Setter Property="IsFloating" Value="True"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeNormal}"/>
    </Style>

    <Style x:Key="MaterialTextBoxDarkStyleLarge" TargetType="{x:Type fwc:MaterialTextBox}" BasedOn="{StaticResource MaterialTextBoxDarkStyle}">
        <Setter Property="FontSize" Value="{DynamicResource FontSizeLarge}"/>
    </Style>
</ResourceDictionary>